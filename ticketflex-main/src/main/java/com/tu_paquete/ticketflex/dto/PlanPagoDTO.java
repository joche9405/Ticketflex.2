/*package com.tu_paquete.ticketflex.dto;

import java.math.BigDecimal;
import java.util.Map;

import com.tu_paquete.ticketflex.Model.Transaccion;

/*public class PlanPagoDTO {
    private String planPagoId;
    private BigDecimal total;
    private BigDecimal cuotaMensual;
    private int numeroCuotas;
    private Transaccion primeraCuota;
    private List<Transaccion> proximasCuotas;
    
    // Getters y Setters
    // Constructor que acepta el Map del servicio
    public PlanPagoDTO(Map<String, Object> planData) {
        this.planPagoId = (String) planData.get("planPagoId");
        this.total = (BigDecimal) planData.get("total");
        this.cuotaMensual = (BigDecimal) planData.get("cuotaMensual");
        this.numeroCuotas = (int) planData.get("numeroCuotas");
        this.primeraCuota = (Transaccion) planData.get("primeraCuota");
        this.proximasCuotas = (List<Transaccion>) planData.get("proximasCuotas");
    }
}*/